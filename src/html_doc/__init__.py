"""html-doc · shared HTML chrome kit + CLI.

Installable as a package: `uv tool install .` (or `pip install .`) gives
you the `html-doc` command on PATH. The same logic lives in
src/html_doc/cli.py; bin/html-doc is a PEP 723-annotated shim that runs
the same `main()` without requiring a global install.
"""

__version__ = "0.2.0"
