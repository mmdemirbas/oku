"""oku · shared HTML chrome kit + CLI.

Installable as a package: `uv tool install .` (or `pip install .`) gives
you the `oku` command on PATH. The same logic lives in
src/oku/cli.py; bin/oku is a PEP 723-annotated shim that runs
the same `main()` without requiring a global install.
"""

__version__ = "0.2.0"
