---
title: Yol haritası
subtitle: Az önce yayımlananlar, hâlâ açık olanlar ve kitin biçimini belirleyen kilitli kararlar.
audience: Bakımcı
order: 999
summary: oku için açık işler, son eklenenler ve kilitlenmiş tasarım kararları.
accent: amber
---

> [!TLDR]
> Grafik kitinin büyük bölümü etkileşimlidir — bilgi balonları, tıklayarak sabitleme, imleç süpürmesi, tam ekranda etkileşimi koruyan lightbox, çakışmayan etiketler, tıklanabilir mini kartlarla aileye göre gruplanmış katalog. Açık işler uzun kuyruktaki gösterge etkileşimi, sürüklenebilir ağ düğümleri, lightbox kaydırma çubuğu + mini harita ve iç içe dillerde kod vurgulama çevresinde yoğunlaşıyor.
>
> - Etkileşim kapsamı: aşağıdaki tablodaki her tür, imleç üzerine gelindiğinde gerçek çapa verisiyle bağlama duyarlı bir bilgi balonu açar. Kartezyen + ridgeline + sparkline + gauge, bilgi balonunu konuma göre güncelleyen eşlenik bir imleç taşır.
> - Görsel incelik: quadrant + bubble etiket çakışma çözümü uçuşa yasak bölgeleri gözetir. Parallel-coordinates çizgi kalınlığı 3.6 → 5px, imleçle birlikte diğerleri solar. Donut/pie'da sabitlenen dilimler büyür ve vurgu çizgisi alır. Mermaid yazı boyutu 13px ile sınırlanır, en-boy uyumu uzun dikey şemaları kırpar.
> - Yerleşim: kaynak / çıktı sütunları reference / charts / tables sayfalarındaki her örnek çiftinde kutu üstünden hizalıdır — piksel farkı sıfır.
> - Bilgi balonu: genişliği sabitleme durumları arasında değişmez; sabitlendiğinde açık bir × kapatma düğmesi çıkar; `_tipPinned` bayrağı imleçle sürülen grafiklerin sabitlenmiş içeriğin üzerine yazmasını engeller.
> - Aileye göre seçim: her biri küçük bir canlı çizim taşıyan 43 tıklanabilir mini kart — okuyucu istediği biçimi görüp doğrudan o türün tam örneğine tıklar.
> - Panonun diğer yarısı sağlamlık: pakete alınmış yazı tipleri, atomik derleme,
>   dinleyicilerin bırakılması, klavyeyle açılan kenar çubuğu katlaması. On üç
>   madde; aşağıda alana ve önceliğe göre süzülebilir.
> - `oku`, PATH üzerinde bir sistem komutu olarak durur (uv tool install).

## Anlık durum {#snapshot}

Kitin bugün nerede durduğu, sayılarla.

```oku-kpi-grid
{"tiles":[{"num":"53","label":"Şemanın kabul ettiği grafik türü"},{"num":"50","label":"Grafikler sayfasında işlenmiş örneği olan tür"},{"num":"19","label":"Kataloglanmış Mermaid şema türü"},{"num":"11","label":"Grafikler sayfasındaki amaç ailesi"},{"num":"20","label":"oku check --strict altında temiz belge sayfası"},{"num":"1500+","label":"Pytest testi — yeşil"},{"num":"2","label":"oku build başına derleme ağacı (standalone / site)"},{"num":"10","label":"Seri renk rampası (--series-1..10)"}]}
```

## Özelliğe göre grafik etkileşimi {#interactivity}

Hangi grafik türünün hangi etkileşim bileşenini taşıdığı. Dolu hücreler yayımlanmış, boş hücreler kuyruktadır. Tablo 53 türün 28'ini kaydeder; kalanlar henüz burada kataloglanmadı.

```oku-table
{"headers":["Tür",{"label":"Aile","filter":"chips","values":["cartesian","categorical","distribution","part-to-whole","flow","hierarchy","matrix","trend","goal","geo"]},"Bilgi balonu","Tıkla-sabitle","İmleçle vurgu","Eşlenik imleç","Tam ekran"],"rows":[["scatter",{"value":"cartesian","values":["cartesian"]},"●","●","●","●","●"],["line",{"value":"cartesian","values":["cartesian"]},"●","●","●","●","●"],["area",{"value":"cartesian","values":["cartesian"]},"●","●","●","●","●"],["bubble",{"value":"cartesian","values":["cartesian"]},"●","●","●","●","●"],["quadrant",{"value":"cartesian","values":["cartesian"]},"●","●","●","—","●"],["bar",{"value":"categorical","values":["categorical"]},"●","●","●","—","—"],["stacked-bar",{"value":"categorical","values":["categorical"]},"●","●","●","—","—"],["grouped-bar",{"value":"categorical","values":["categorical"]},"●","●","●","—","—"],["donut",{"value":"part-to-whole","values":["part-to-whole"]},"●","●","●","—","●"],["pie",{"value":"part-to-whole","values":["part-to-whole"]},"●","●","●","—","●"],["waffle",{"value":"part-to-whole","values":["part-to-whole"]},"●","●","○","—","●"],["treemap",{"value":"hierarchy","values":["hierarchy"]},"●","●","○","—","●"],["heatmap",{"value":"matrix","values":["matrix"]},"●","●","○","—","●"],["histogram",{"value":"distribution","values":["distribution"]},"●","●","○","—","●"],["sparkline",{"value":"trend","values":["trend"]},"●","●","—","●","●"],["gauge",{"value":"goal","values":["goal"]},"●","●","—","●","●"],["radar",{"value":"categorical","values":["categorical"]},"●","●","○","—","●"],["box-plot",{"value":"distribution","values":["distribution"]},"●","●","○","—","●"],["bullet",{"value":"goal","values":["goal"]},"●","●","○","—","●"],["slope",{"value":"trend","values":["trend"]},"●","●","○","—","●"],["calendar-heatmap",{"value":"trend","values":["trend"]},"●","●","○","—","●"],["ridgeline",{"value":"distribution","values":["distribution"]},"●","●","○","●","●"],["sankey",{"value":"flow","values":["flow"]},"●","●","○","—","●"],["network",{"value":"flow","values":["flow"]},"●","●","○","—","●"],["scatter-matrix",{"value":"matrix","values":["matrix"]},"●","●","○","—","●"],["parallel-coordinates",{"value":"matrix","values":["matrix"]},"●","●","●","—","●"],["chord",{"value":"flow","values":["flow"]},"●","●","○","—","●"],["geo",{"value":"geo","values":["geo"]},"●","●","○","—","●"]]}
```

> [!NEUTRAL] Simge anahtarı
> ● yayımlandı · ○ kuyrukta · — uygulanmaz. Bilgi balonu ve tıkla-sabitle, yukarıda kayıtlı 28 türün tamamında yayımlanmış durumda. İmleçle vurgu (bir öğenin üzerine gelindiğinde ötekilerin solması) Kartezyen olmayan türlerin çoğunda hâlâ kuyrukta. Eşlenik imleç yalnızca süpürme ekseni olan grafiklerde anlamlıdır (Kartezyen, ridgeline, sparkline, gauge). Tam ekran lightbox, SVG grafiklerinde grafik etkileşimini korur; bar türevleri DIV tabanlıdır ve bunun yerine sayfanın olağan kaydırmasını kullanır.

## Açık / sürmekte {#open}

Kanban panosu — kapsamı daraltmak için alana ya da önceliğe göre süzün. Her çip süzer; pano görünümü duruma göre gruplar.

```oku-table
{"view":"board","headers":["Madde",{"label":"Durum","boardOrder":["in-flight","queued","exploring","shipped"]},{"label":"Alan","filter":"chips","values":["charts","diagrams","tables","kit","docs","build"]},{"label":"Öncelik","filter":"chips","values":["P1","P2","P3"]},{"label":"Emek","filter":"chips","values":["S","M","L"]}],"rows":[["Network — okuyucunun grafiği elle çözebilmesi için sürüklenebilir düğümler",{"value":"queued","values":["queued"]},{"value":"charts","values":["charts"]},{"value":"P1","values":["P1"]},{"value":"M","values":["M"]}],["Bütün grafik türlerinde etkileşimli gösterge (donut/pie/waffle/radar/box-plot — Kartezyen + bar davranışına eşitlensin)",{"value":"queued","values":["queued"]},{"value":"charts","values":["charts"]},{"value":"P2","values":["P2"]},{"value":"M","values":["M"]}],["İç içe kod vurgulama (HTML içinde JS, Markdown içinde bash — çitli blok içinde alt dil algılama)",{"value":"queued","values":["queued"]},{"value":"kit","values":["kit"]},{"value":"P2","values":["P2"]},{"value":"M","values":["M"]}],["Lightbox: sığmayan büyütülmüş grafikler için kaydırma çubuğu + resim içinde resim mini haritası",{"value":"queued","values":["queued"]},{"value":"kit","values":["kit"]},{"value":"P2","values":["P2"]},{"value":"M","values":["M"]}],["Kartezyen olmayan grafiklerde imleçle vurgu (ötekileri soldurma) — donut/pie/waffle/treemap/heatmap vb.",{"value":"queued","values":["queued"]},{"value":"charts","values":["charts"]},{"value":"P3","values":["P3"]},{"value":"S","values":["S"]}],["Katalog araştırması — waterfall, marimekko, dot-matrix, joy-plot, sunburst",{"value":"exploring","values":["exploring"]},{"value":"charts","values":["charts"]},{"value":"P3","values":["P3"]},{"value":"M","values":["M"]}],["Renderer'ın GFM ayrıştırıcısında dipnotlar + başvuru biçimli bağlantılar (b[] dizeleri boyunca sayfa düzeyinde durum)",{"value":"shipped","values":["shipped"]},{"value":"kit","values":["kit"]},{"value":"P3","values":["P3"]},{"value":"M","values":["M"]}],["Tepkisel metin: ${expr} yerleştirme + form girdilerinde oku-bind çift yönlü bağlama (eklemeli, v3'ü değiştirmeden oturur)",{"value":"exploring","values":["exploring"]},{"value":"kit","values":["kit"]},{"value":"P3","values":["P3"]},{"value":"L","values":["L"]}],["Inter ve JetBrains Mono yazı tiplerini pakete al; bugün her sayfa açılışta Google'dan çekiyor",{"value":"shipped","values":["shipped"]},{"value":"build","values":["build"]},{"value":"P1","values":["P1"]},{"value":"M","values":["M"]}],["Derlemeyi atomik yap: yeni ağaçları eskinin yanına yazıp yer değiştir, böylece hata eski çıktıyı bozmaz",{"value":"shipped","values":["shipped"]},{"value":"build","values":["build"]},{"value":"P1","values":["P1"]},{"value":"M","values":["M"]}],["İşlenen bir .md içindeki betiğin ne yapabileceğine karar ver — görüntüleyici bugün onu çalıştırıyor",{"value":"shipped","values":["shipped"]},{"value":"kit","values":["kit"]},{"value":"P1","values":["P1"]},{"value":"M","values":["M"]}],["Özel elemanlar bağladıklarını bıraksın: ışık kutusu kaydırma/yakınlaştırma, sütun genişletme, grafik yeniden ayarı, kaydırma takibi",{"value":"shipped","values":["shipped"]},{"value":"kit","values":["kit"]},{"value":"P2","values":["P2"]},{"value":"M","values":["M"]}],["Kenar çubuğu bölüm katlaması klavyeyle erişilebilir olsun (başlık odak almıyor, durum taşımıyor)",{"value":"shipped","values":["shipped"]},{"value":"kit","values":["kit"]},{"value":"P2","values":["P2"]},{"value":"S","values":["S"]}],["Çizilecek satırı olmayan grafik yükü boş çerçeve yerine durumu söylesin",{"value":"shipped","values":["shipped"]},{"value":"charts","values":["charts"]},{"value":"P2","values":["P2"]},{"value":"S","values":["S"]}],["island-hand-styled mermaid bloklarını da gezsin — hex yazılmış bir classDef aynı kusur",{"value":"shipped","values":["shipped"]},{"value":"docs","values":["docs"]},{"value":"P2","values":["P2"]},{"value":"S","values":["S"]}],["İki kaynak gösterme ögesi seri belirteci yerine hex sabiti boyuyor",{"value":"shipped","values":["shipped"]},{"value":"kit","values":["kit"]},{"value":"P2","values":["P2"]},{"value":"S","values":["S"]}],["Proje sınırının dışındaki görsel sessizce gömülmesin, denetimde bildirilsin",{"value":"shipped","values":["shipped"]},{"value":"build","values":["build"]},{"value":"P2","values":["P2"]},{"value":"S","values":["S"]}],["Yazarın dizinini taşımayan bir künye: makineden çıkan sayfa için bir kapatma seçeneği",{"value":"shipped","values":["shipped"]},{"value":"build","values":["build"]},{"value":"P2","values":["P2"]},{"value":"S","values":["S"]}],["Adında # veya ? olan dosya, hiçbir bağlantının ulaşamayacağı bir sayfaya dönüşüyor",{"value":"shipped","values":["shipped"]},{"value":"build","values":["build"]},{"value":"P3","values":["P3"]},{"value":"S","values":["S"]}],["Sayfanın anatomisi v1 JSON değil v3 markdown biçimini anlatsın",{"value":"shipped","values":["shipped"]},{"value":"docs","values":["docs"]},{"value":"P2","values":["P2"]},{"value":"S","values":["S"]}],["Tarayıcı testleri saate değil koşula baksın (geriye 88 çağrı, 41 sn sabit bekleme kaldı; 212 çağrı ve 205 sn idi)",{"value":"in-flight","values":["in-flight"]},{"value":"kit","values":["kit"]},{"value":"P3","values":["P3"]},{"value":"M","values":["M"]}],["Görüntülenen belgedeki bir `<style>`, onu açan sayfanın biçimini değiştiriyor",{"value":"shipped","values":["shipped"]},{"value":"kit","values":["kit"]},{"value":"P3","values":["P3"]},{"value":"S","values":["S"]}],["Sütun genişletme tutamağı imlece 6 pikselinin 2'sini veriyor, gerisi yandaki hücrenin",{"value":"shipped","values":["shipped"]},{"value":"tables","values":["tables"]},{"value":"P2","values":["P2"]},{"value":"S","values":["S"]}]]}
```

## Son eklenenler {#recent}

- Okur artık belgeyi büyütebiliyor ve büyüyen şey belgenin tamamı. %80 ile %200 arasındaki yazı boyutu basamakları okuma sütununa `zoom` uyguluyor; yazıyı, gömülü SVG'yi, Mermaid çıktısını ve görselleri birlikte büyütüp yine de satırları yeniden saran tek yöntem bu — 1.5 katında paragraf 1.5 katında sarıyor, gerilmiş bir resim olmuyor. 1440 piksellik ekranda bu deponun kendi grafik sayfasında ölçüldü: aynı metin 1.0'da 145 piksel, 1.5'te 218 piksel genişlikte çiziliyor; bir grafiğin yüksekliği 55 pikselden 97 piksele, kenar çubuğundaki bir ağaç satırı 28 pikselden 42 piksele çıkıyor; 44 piksellik denetim düğmeleri, üst şerit ve kenar çubuğunun 320 piksellik genişliği ise hiç kıpırdamıyor. Üç şeyi bozdu, bir şeyi de görünür kıldı. `max` kipindeki `--content-width: 100vw` 1.25 katında görüntü alanının %125'ini istedi, çünkü görüntü alanı birimi `zoom`'un dokunmadığı bir kapsayıcıya göre çözülüyor; artık `100%`. Büyütülmüş sütunun içindeki `position: fixed` bir ipucu kutusu büyütmeyi de beraberinde taşıyor — `left` alanına yazılan koordinat çarpılıyor, oysa o koordinat çarpma yapmayan `getBoundingClientRect()` çağrısından geliyordu — bu yüzden grafik ipucu 1.25 katında çubuğundan 189, 1.5 katında 360 piksel uzağa düştü; `main` içinde yaşayan üç ipucu artık `__okuSetFixedPos` üzerinden yazıyor. `main`'in iç boşluğu ölçeğe bölünüyor, çünkü telefonda sütun zaten görüntü alanına sıkışmış durumda ve boşluğun kazandığı her piksel şekilden gidiyordu: okur DAHA fazlasını isterken bir grafik 286 pikselden 256 piksele düşüyordu. Görünür kılınan sorun ise bu işten eski: `minmax(200px, 1fr)` bir izin altına inmeyi reddettiği bir taban, dolayısıyla 360 piksellik ekranda 2 katında sekiz karşılaştırma kartı 360 pikselin içine 400 piksel genişliğinde yerleşti — artık her otomatik doldurma tabanı `minmax(min(Npx, 100%), 1fr)` ve azami içeriğe göre boyutlanıp `max-width: 100%` taşımayan bir mini grafik satırı da onunla birlikte düzeltildi.

- Sunumla ilgili her seçim tek bir düğmenin arkasında. Esnek satır köşenin aritmetiğini düzeltmişti, sayısını değil: o köşeyi altı düğme istiyordu — kişiselleştirme, arama, dil, uyarı, tema, genişlik — ve yazı boyutu yedincisi olacaktı. Okuma sütununun üstünde duran altı adet 44 piksellik kutu, okurun her tıklamadan önce okuduğu bir araç çubuğudur ve bunun beşte biri dışındaki her şey bir kez ayarlanır. Ayrım sıklığa göre yapıldı: arama dışarıda kalıyor (okurken kullanılıyor), uyarı göstergesi de öyle (bir alarmdır ve kendini asla gizlememelidir); yazı boyutu, genişlik, tema, dil ve yer tutucular menüde birer satır. Kazanç iki katlı: yalnızca simgeden oluşan bir döngü düğmesi, 44 piksellik kutuda söze yer olmadığı için vardır — genişlik denetiminin üç bölmeli simgesi okunmak için üzerine gelinmeyi gerektiriyor ve duraklarını kimsenin açmadığı bir `title` içinde duyuruyordu. Bir satırda her durak için ayrı bir etiketli düğmeye yer var, böylece durum duyurulmak yerine görünüyor. Satırlar listelenmek yerine kendilerini kaydediyor, esnek satırın var olma nedeniyle aynı gerekçeyle — dil yalnızca çevirisi olan sayfada, yer tutucular yalnızca bir kod parçası tanımlamışsa görünüyor — ve içindeki bir denetim kullanıldığında panel kapanmıyor, çünkü doğru yazı boyutunu bulmak üç tıklama ve ilkinde kapanan bir menü bunu dokuza çıkarır.

- Metin kuralları artık yalnızca paragrafları değil, metnin tamamını okuyor. `placeholder-text` yalnızca paragraflara bakıyordu; oysa aynı işlevin üç satır ötesindeki `process-breadcrumb`, türü belirtilmiş yüklerin içine gömülü metni de geziyordu. Aynı `TODO` sözcüğünü hem bir paragrafta hem bir adım gövdesinde taşıyan tek bir sayfada ölçüldü: tek uyarı, paragraftan. Bu deponun kendi `examples/iceberg.md` dosyası, bir karşılaştırma kartında `Mermaid diagrams (still TODO in build sequence step 11)` satırını taşıyordu ve `oku check --strict` denetiminden geçiyordu — o kartın gelecek işi diye saydığı üç şeyin üçü de bu arada tamamlandı. Kart, kitin bugün yaptıklarını anlatacak biçimde yeniden yazıldı; boşluğun gizlediği şey işte o bayat metindi. İki kural da HTML adacığının kapısında duruyordu: metin döngüsünün en altında, üç `continue` ifadesinin ardında oturuyorlardı, bu yüzden bir `<div>`'in etiketleri arasındaki hiçbir şey okunmuyordu — oysa adacık, okurun içinden gördüğü bir işaretlemedir. İstisna ham metin öğeleridir; bunların takibi adacık yürüyüşüne bırakılmadı, çünkü o yürüyüş yalnızca adacığın açılış etiketinde devreye giriyor: kitin adacık içinde çok satırlı kod için belgelediği `<div>` içindeki `<pre>` kalıbı onun için sıradan bir adacık satırıydı, oysa bir örnekteki `// TODO: implement` satırı örneğin kendisidir. İlk boşluğu kapatmak öbür yarısını ortaya çıkardı: `docs/cli.md` kendi denetiminden kaldı, çünkü önem tablosunun yakaladığı dört yer tutucuyu yazması gerekiyor ve o tablo bir yüktür. Kod aralığındaki bir belirteç, adı anılan belirteçtir; bu yüzden yer tutucu araması öncesinde kod aralıkları maskeleniyor — silinmiyor, boşlukla maskeleniyor, çünkü denetleyicideki her konum bilgisi kaynağın yazıldığı hâlindeki satır numarasıdır ve bir aralığı silmek ardındaki her şeyi kaydırır.

- Konusu doğrudan bir geçmiş olan sayfa artık o geçmişi anabiliyor. `process-breadcrumb` kuralı kapsamını metni KİMİN yazdığına göre belirliyordu — depo markdown'ından üretilen sayfalar muaftı, çünkü o metin kite ait değil — oysa daha önceki bir turu düzelten, elle yazılmış bir ölçüm raporu yazarına aittir ve yine de kit belgesi değildir. Kaçış yolu yoktu: düzelttiği turu anan her cümle bir uyarı doğuruyor, `oku check --strict` 1 ile çıkıyor ve kapıdan geçmenin tek yolu her göndermeyi tarihe çevirmek oluyordu; bu da okuyucunun ihtiyaç duyduğu göndermeyi yok ediyordu. Tek bir belgede 29 uyarı ölçüldü, hepsi doğru metin üzerinde. Ön bilgideki `documents_history: true`, yalnızca o sayfada yalnızca o kuralı devre dışı bırakır. Çözüm gibi görünen `skip_prose` dalına katılmadı: o dal bütün metin kurallarını kapatır, oysa bir raporda unutulmuş yer tutucu hâlâ yer tutucudur — ölçüldü, muafiyet altında `placeholder-text` uyarısı çıkmaya devam ediyor. kit.json ayarı da yapılmadı; `skip_gitignored`'ın kendisi için söylediği neden burada da geçerli: ağaç genelinde bir ayar, sonradan yazılan sayfaları da, metin yazarken kimsenin açmadığı bir dosyadan muaf tutar. İki uyarı kaynağı da bayrağa uyuyor, çünkü aynı cümle bir adım gövdesinde başka bir çağrı noktasından aynı kodu doğuruyordu. Uyarı, çıkış yolunu adıyla söylüyor; çünkü doğru bir metin üzerinde çıkan ve adı konmuş bir çıkışı olmayan uyarıyı yazar, göndermeyi silerek aşar.

- Ön bilgi belirtiminin belgelediği yedi vurgu renginden dördü hiçbir zaman bir renk olmadı. `_applyAccent` içindeki palet eşlemesi üçünü taşıyordu; `violet` ve `green`, `--accent` değerini tarayıcının kendi renk adları tablosundan alan bir yedek dala düşüyor, `--accent-soft` ile `--accent-strong` ise indigo'da kalıyordu — yani bir bilgi kutusu çizgisinde bir renk, arkasındaki yüzeyde başka bir renk beliriyordu. `rose` ve `slate` ise CSS renk adı değil; bildirim geçersiz kalıyor, `--accent` düz metnin kendisine çözümleniyor ve `buildConfig` bu metni somut bir renk bekleyen Mermaid'e veriyordu. Örnek üzerinde ölçüldü: `accent: teal` 155 piksellik bir şekil çizdi, `accent: rose` 0 piksel çizdi ve sayfadaki her şema için "Unsupported color format" kartı bastı; `oku check --strict` her iki sayfayı da temiz saydı. Artık yedisinin de açık ve koyu tema aileleri var ve her biri hem sayfa arka planında hem kendi zemininde iki temada da 4,5:1 oranını geçiyor (ölçülen en düşük değerler: amber'in zemin üstü koyu tonu 4,51, green'in arka plan üstü vurgusu 4,80). Eşlemede olmayan bir değer önce tarayıcıya sorulur; böylece `rebeccapurple` ve `hsl(280 60% 40%)` tek bir renk yerine uyumlu bir açık, koyu ve gece varyantı alır. Tarayıcının çözemediği bir değerde sayfa varsayılan vurguyu korur ve bunu bildirir; çünkü yanlış bir renk telafi edilebilir, hata kartlarıyla dolu bir sayfa edilemez. `oku check` artık ikisinden de olmayan düz bir sözcük için `accent-unknown`, denetlenen kitin tanımlamadığı bir `var(--token)` değerini adlandıran mermaid `classDef` satırı için `diagram-unknown-token` diyor — başka bir projeden gelen bildirimin biçimi tam olarak buydu: beceri dosyası `var(--series-N-soft)` kullanmayı söylüyor, kurulu araç ise onları gönderen kitin üç hafta gerisindeki `kit 2026-08-20-r50` sürümünü okuyordu.
- Şema etiketindeki eğri tırnak bir sınırlayıcı değil, sıradan bir karakterdir. Şema öğesi kaynağını Mermaid ayrıştırmadan önce çözer; çünkü bir çitin gövdesi markdown hattından kaçış karakterleriyle gelir — `<` için `&lt;`, ok için `--&gt;`; geçişin var olma nedeni budur. Eğri tırnağın da bir kaçış artığı olduğu okumasıyla yanına iki dönüşüm daha eklenmişti ve bunlar `A["the “quoted” label"]` ifadesini `A["the "quoted" label"]` hâline getiriyordu; düz tırnaklar dizgiyi bir sözcük sonra kapatıyordu. Yazarın bir düğüm içine yalnızca tipografik metin yazdığı bir sayfada ölçüldü: altı etiket, hiç şekil yok, `Parse error on line 2` ve tertemiz bir `oku check --strict`. Bu dönüşüm Mermaid'in kabul ettiği bir etiketi ancak kabul etmediği bir etikete çevirebilirdi; bu yüzden daraltılmadı, kaldırıldı. Görünür sınırlayıcı olarak kullanılan eğri tırnaklar zaten ayrıştırılıyor — etiketin kendi metni olarak. Derlenmiş tek bir sayfadaki altı durum her iki yarıyı da tutuyor: üç eğri tırnak, üç kaçış dizisi; ok, temizlik daraltılmak yerine tümüyle silinseydi düşecek olan durumdur.
- `oku --version` artık yalnızca "bu farklı bir yapı mı" sorusunu değil, "bu yapı düzeltmeden eski mi" sorusunu da yanıtlıyor. Önceden `src 8cd13843bf28` basıyordu: karşılaştıran ama sıralamayan bir içerik özeti; üstelik on iki çıplak onaltılık karakter, yani kısaltılmış bir git commit görünümünde. Bir işleme kusuru elinde olan okuyucu bunun üzerinde `git cat-file -t` çalıştırdı, "Not a valid object name" yanıtını aldı ve sonunda site-packages içinde arama yapıp dist-info zaman damgasına bakarak 11:53'teki kurulumunun aynı gün 16:38'de işlenen düzeltmeden önce olduğunu çıkardı. Satır şimdi `src sha256:<12>` ve `as of <tarih>` taşıyor — tarih, özetin kapsadığı dosyalar arasındaki en yeni değişiklik zamanı; ikisi tek bir dosya listesinden okunuyor, çünkü farklı bir kümeyi kapsayan bir tarih başka bir yapıyı anlatır. Bir derleme kancasıyla damgalanmak yerine dosyalardan türetiliyor, yani ne git'e ne paketleme düzeneğine ihtiyaç duyuyor: kurulu bir araçta kurulum zamanı, çalışma kopyasında son düzenleme zamanı olarak okunur.

- Arama alıntısı artık eşleşen sözcüğü işaretliyor. `toLowerCase()` uzunluğu korumaz — Türkçe `İ`, `i` artı bir birleşen nokta olur — bu yüzden aramanın küçük harfli gövdede bulduğu konum, sonra dilimlenen gövdedeki konum değildi. Türkçe bir bölümde `kayıt` araması ölçüldü: işaret `ayıt ` diyordu ve pencere eşleşmeden 40 değil 37 karakter önce açılıyordu. Aynı bölümde daha önce küçük harfi kendisinden uzun olan bir karakter geçmedikçe kayma tam olarak sıfırdır; hatanın bunca zaman yaşamasının nedeni bu — yalnızca yazarı Türkçe yazan sayfalarda görünüyor. Küçük harfli metin ile geri eşleme artık tek geçişte ve yalnızca küçük harfe çevirme uzunluğu değiştirdiğinde kuruluyor, yani İngilizce yol tek bir karşılaştırma ödüyor. İşaretin alıntı içindeki yeri ikinci bir arama değil, aritmetik: ikinci bir `indexOf`, yanlış karaktere düşmek için ikinci bir şanstır ve pencere eşleşmeyi kaçırdığında hiçbir şey bulamıyordu. Aynı kusur, `html.lower().find("</script")` arayan bir sınama yardımcısında da çıktı; hiçbir kit dosyasında `İ` bulunmadığı sürece doğru çalışmıştı.
- Bir başlığın kimliği artık tek bir kural; İngilizcede uyuşan üç kural değil. cli.py içindeki `_md_slug`, renderer.js ve chrome.js içindeki `slugify` bunu ayrı ayrı belirliyordu ve sonuçların hepsi sessizdi: `## Özet` `zet` kimliğini alıyordu, `## 概述` boş dizge alıp üstüne bir bölüm eklendiğinde değişen sıra tabanlı `sec-3`'e düşüyordu, tek bölüm altındaki iki Çince h3 aynı kimliği alıyordu ve Python yanıtını hesaplayan `oku check` `[Özet](#özet)` bağlantısını onaylıyor, bağlantı ise tarayıcıda hiçbir yere gitmiyordu. Ters yönde ise denetim, DOM kimlikleri farklı olan bir h2 ile bir h3 arasında `duplicate-anchor` bildiriyor ve doğru işlenen bir sayfayı derlemeyi reddediyordu. Belgelenen kaçış yolunda da aynı kusur bir kat yukarıda vardı: işleyicinin `{#id}` örüntüsü JavaScript'in ASCII `\w` sınıfını kullanıyordu, bu yüzden `{#a-özet}` başlığın içinde düz metin olarak görünüyordu. Kural şu: küçük harfe çevir, NFC ile birleştir, harf, sayı, boşluk, `_` ve `-` dışındaki her şeyi at, sonra boşluk ve tire dizilerini `-` yap. Birleştirme, aynı başlığın hangi normalleştirme biçiminde yazıldığından bağımsız olarak tek bir kimlik vermesini sağlıyor; kalan işaretleri atmak ise küçük harfe çevrilmiş `İ` harfini bir Türkçe yazarın bağlantıya yazacağı düz `i` hâline getiriyor. Var olan hiçbir sayfanın kimliği kaymıyor; kaydırabilecek iki değişiklik denetim tarafında yapıldı. Otuz durum var, on yedisi eski kitte başarısız; hepsi uçtan uca ölçüyor: dokuz yazı sistemiyle başlıklar taşıyan bir sayfa derlenip işleniyor ve Python'ın öngördüğü kimlik DOM'dan okunuyor.
- Beş kutu daha en uzun sözcüğünden dar olabiliyor. Bir adım akışı kartındaki uzun bir sınıf adı 360 pikselde sayfayı yana kaydırdığında `minmax(0, 1fr)` on beş ızgara şablonunu düzeltmişti; kuralı sonra akla gelen ilkellere uygulanmıştı. Kalanları bulmanın yolu biçem sayfasını okumak değildi: `kit/schema/examples.json` içindeki her örnekten, taşıdığı her dizgeye uzun belirteç eklenerek kurulan bir sayfa. KPI sayısı satır içi blok olduğu için azami içeriğe göre boyutlanıyor, yani `1.2M req/s` gibi bir değer karonun dışına taşıyor — bu, MASAÜSTÜNDE taştı: 1440 piksellik görünüm alanına karşı 1818. Açıklamalı kod gövdesi ile zaman çizelgesi başlığı `min-width: auto` taşıyan esnek ögeler; zaman çizelgesi rozetinde `white-space: nowrap` vardı ve bunu `overflow-wrap` kurtaramaz, çünkü sarma kapalıyken alınacak bir kırılma noktası yoktur; canlı parçacık etiketi de sıfırlama düğmesini kenardan dışarı itiyordu. 360 piksellik sayfada kaydırma genişliği 948'di ve görünüm alanının dışında dokuz öge vardı; şimdi 360 ve hiçbiri yok. 1440 pikselde 1818'di, şimdi 1440. Sınama artık masaüstünü de ölçüyor — önceden var olan iki sınama yalnızca dar görünümlere bakıyordu, yani masaüstündeki okur yana kaydırırken yeşil kalırlardı.
- Bir grafik artık tarayıcının çizmeyi reddettiği bir işaret üretmiyor. Chrome negatif bir `width` değerini kırpmaz; dikdörtgeni büsbütün atar ve `<rect> attribute width: A negative value is not valid` satırını yazar. Böylece şekil başlığını, eksenlerini ve ölçek etiketlerini korur, yalnızca veriyi kaybeder; konsol ise grafiği değil bir öz niteliği adlandırır. Buraya iki yük ailesi ulaşıyor ve ikisi de olağan işten geliyor: boşluğun, o boşluktan daha ince bir hücreden çıkarılması — 576 piksellik bir alana sığdırılan 900 aralığın her biri 0,64 piksel eder, 1 piksellik çubuk arası boşluk genişliği -0,36'ya indirir — ve ters yazılmış bir çift (`q1` altında `q3`, `low` altında `high`, `start` öncesinde `end`), ki burada aralık ölçeklenmiş iki koordinatın farkıdır. `markSpan` çifti sıraya koyuyor ve sonucu on çağrı yerinde 0,5 piksele indiriyor; ikinci aile için `oku check` `chart-inverted-range` bildiriyor, çünkü çifti sıraya koymak onu gizleyen şeyin ta kendisi: şekil doğru görünür, yanlış okunur. Okuyarak değil, gönderilen her örneği dört yönde değiştirerek bulundu: tek sayfadaki 244 yük, dört bloktan 904 konsol hatası üretti; sonrasında hiç. Her olağan grafik 1440 ve 360 pikselde öncesiyle aynı ölçüyor; 900 aralıklı histogramın 900 çubuğu 0 genişlikten 0,74 genişliğe geçiyor.
- Tarayıcı testleri artık büyük ölçüde koşula bakıyor. Toplamı 205 saniye tutan 212 `wait_for_timeout` çağrısı vardı — erken biten her koşuda ölü zaman, bitmeyen tek koşuda ise kırılganlık; üstelik kırılganlık, zamanlamayla hiç ilgisi olmayan bir doğrulamada hata olarak görünüyordu. Bunların 124'ü `page_quiet` (işleme geçişi belgeyi değiştirmeyi bıraktı), `box_stable` (geçiş bitti), `scroll_stable`, `diagram_drawn` ya da `until_changed` oldu. Arka arkaya iki tam koşu: 636 durumun tamamı 339,7 ve 331,8 saniyede geçti; öncesi 608 saniyeydi. Geriye 88 çağrı ve 41 saniye kaldı; her biri bir imleç üzerine gelmesini, dokunmayı ya da imlecin ayrılmasını bekliyor — teste özgü bir koşul gerekiyor ve tahmin edilmiş bir koşul, beklemeden daha kötüdür, çünkü yanlış nedenle geçer.
- "Bir sayfanın anatomisi", yazarın gerçekten yazdığı biçimi gösteriyor. Bölüm v1 JSON sayfasıyla açılıyordu — `kind: "page"`, bir `meta` nesnesi, bir `blocks` ağacı — yani okurun karşılaştığı ilk örnek, başvurmaması gereken biçimdi; markdown ise daha aşağıda, ek bir olanak gibi sunulan bir alt bölümdü. Örnek artık ön bilgi ve içinde türlü çit bulunan bir GFM gövdesi; ardından biçimin tamamını oluşturan üç madde geliyor, JSON ise yanında `oku migrate` ile birlikte eski biçim olarak anılıyor. Sekiz içerik testi bunu iki dilde de tutuyor; eskisinde sekizi de başarısız oluyor.
- Adında URL ayracı geçen dosyanın sayfasına ağaçtan ulaşılabiliyor. Her manifesto `path` değeri bir bağlantı adresine dönüşüp `location.pathname` ile karşılaştırılır; tarayıcı onu yüzde kodlamasıyla verir. Ham hâlde `notes#1.html` `notes` sayfasını ve `1` parçasını gösteriyordu, `what?.html` bir sorgu dizesini, `a b.html` ise llms.txt bağlantısının hedefini boşlukta bitiriyordu. Sayfa her seferinde üretiliyor, dizine giriyor, listeleniyor ve onu adlandıran satır başka yere gidiyordu — hiçbir şey hata vermiyordu. Yollar artık parça parça kodlanıyor, `parent` da onlarla birlikte (bir yol ile altında gruplandığı üst yol dizge olarak karşılaştırılır) ve tam olarak bir kez; `source` ise dosyayı düzenleyicide açacak okur için ham kalıyor. Yedi tarayıcı durumunun beşi, yedi Python durumunun dördü düzeltme olmadan başarısız; boşluk durumu iki hâlde de geçiyor, çünkü onu tarayıcı kendisi kodluyor.
- Görüntülenen belgedeki biçem sayfası o belgeye sınırlandı. `<style>` çalışmaz — ilk etkisizleştirme geçişinin onu bırakma nedeni buydu — ama eklendiği belgenin tamamına, yani okurun sayfasına ulaşıyordu. Bağlantı verilen dosyadaki `body { display: none }` o sayfayı geri almaya yarayacak hiçbir denetim bırakmadan boşaltıyordu; düzeltme olmadan test modülünün kendi hazırlığı bile görüntüleyiciye ulaşamıyor, on beş testin tamamı başarısız olmak yerine hata veriyor. Biçem sayfası artık görüntüleyici gövdesine bağlı tek bir CSS iç içe bloğuna sarılıyor; belge kendi biçemini koruyor, `:root`, `html` ya da `body` adlarını kullanan kural ise hiçbir şeyle eşleşmiyor. `<link>` çerçeve ögeleriyle birlikte gidiyor: başka kökenli bir biçem sayfasının sınırlandırılacak kuralı yoktur ve her `rel`, yalnızca bir dosyayı açan okur adına yapılmış bir ağ isteğidir. Bildirim ikisini de sayfanın kendi dilinde sayıyor — dizgi tablosuna beş yeni anahtar.
- Makineden çıkacak bir sayfa yeniden üretme komutundan vazgeçebilir. Alt bilgideki `cd ~/code/notes && oku build` bir hesabı değil bir dizin düzenini adlandırır; yine de sayfanın ulaştığı herkese verdiği bir bilgidir. kit.json içindeki `"rebuild_command": false` alanı kaldırır, düğme de onunla gider — çalışma zamanı `cmd` yokken zaten hiçbir şey çizmiyordu, yani kapalı bir düğme durumu yok. Sürüm, kit damgası, derleme yaşı ve sapma uyarısı kalır; hiçbiri bir makineyi adlandırmaz. Varsayılan açık: alt bilginin var olma nedeni, ne kadar eski olduğunu söyleyemeyen bir sayfaydı.
- Projenin dışına uzanan görsel olduğu yerde bırakılıyor ve adı veriliyor. `collect_page_assets` dosyanın derlenen ağacın üstünde olup olmadığını soruyordu; projenin ona sahip olup olmadığını soran yoktu, bu yüzden `![x](../../baska-depo/ekran.png)` sessizce tek dosyalık sayfaya gömülüyordu, üstelik `oku check` tertemiz geçerek. Sınır, `#f/` göndermelerinin zaten uyduğu sınır — en yakın `.git`, yoksa `kit.json` — ve derlemenin taşırken kullandığı tarayıcıyla okunuyor, yani denetim tam olarak derlemenin yayımlayacağı şeyi bildiriyor. Uyarı olarak `image-outside`; derleme almadığı dosyayı yazdırıyor; gönderme sayfada kalıyor. Belge kökünün üstünde ama proje içinde kalan görsel taşınmaya devam ediyor; sınırı topyekûn bir redde değil, buna ihtiyaç duyulduğu için var.
- `oku check`'in ürettiği her kod, önem tablosunun her iki dilde de açıkladığı bir kod. Tek bir listenin üç kopyası ve birbirlerini görme yolu yok — `test_authority_agreement.py`'nin var olma nedeni olan sekizinci kaynak. İki yön de tutuluyor; ters yön, dört çağrı biçiminden ikisini okuyan bir taramanın `chart-*`, `json-parse-failed` ve `shadowed-source` kodlarını belgeli-ama-ölü saydığını gösterdi.
- Hex yazılmış bir mermaid `classDef` de `island-hand-styled` sayılıyor. Denetim yalnızca dize bloklarını geziyordu; mermaid çiti oraya varmadan `diagram` bloğuna dönüştüğü için bu deponun kendi mimari sayfasındaki dokuz sabit dolgu `oku check --strict` denetiminden temiz geçti — koyu sayfada da açık kalan soluk zeminler, üstlerinde temayla birlikte ters dönen bir etiketle. Rampaya her renk ve her tema için bir zemin eklendi (`--series-1-soft` … `--series-10-soft`), şemalar `fill:var(--series-N-soft),stroke:var(--series-N),color:var(--text)` biçimine taşındı. Zemin artık temayla dönüyor (1280 pikselde de 360 pikselde de `rgb(220,252,231)` ile `rgb(20,83,45)` arası) ve üstündeki etiket açık temada 12,47–15,56:1, koyu temada 7,10–9,28:1 ölçüyor. Mermaid'in dilbilgisi CSS işlevlerini değil değerlerini aldığı için zeminler çağrı yerinde bir `color-mix()` değil, birer belirteç.
- Çizecek verisi olmayan grafik artık yalnızca derleme sırasında değil, çizim sırasında da durumu söylüyor. `oku check` boş yükü tek kurallı geçişten beri geri çeviriyordu; işleyici yine de çerçeveyi ve eksenleri çiziyordu ve genel boş blok koruması bunu geçiriyordu, çünkü eksen etiketleriyle dolu bir `<svg>` görünür sayılıyor. Denetimin ulaşamadığı iki durum, elle düzenlenmiş bir JSON sayfası ile eski bir araçla üretilmiş bir ağaç. Aynı kural artık iki yerde de, ikisinde de yazarın yazdığı yük üzerinde çalışıyor — `line` türü normalleştirilince `marks` doluyor ve bu veri gibi görünüyor.
- İçindekiler panelindeki bölüm artık klavyeyle katlanıyor. Denetim bir `<span>`di ve tıklama işleyicisi satırdaydı; bölüm başlığında klavyeyle ulaşılabilen tek şey bağlantıydı, o da sayfada gezindiriyordu. Artık `aria-expanded` ve `aria-controls` taşıyan bir `<button>`; kaydırma takibiyle tek bir ayarlayıcı üzerinden aynı durumda tutuluyor. Katlanacak yeri olmayan bölümde düz bir span kalıyor, böylece sayılar hizasını koruyor (bağlantı x 66; hem 1440 hem 360 piksel genişlikte) ve hiçbir yere gitmeyen bir sekme durağı eklenmiyor.
- Sütun genişletme tutamağı artık kendi hücresinin içinde. Sınıra binen eski hâlde (`right: -3px`) yandaki `th` dış üçte ikiyi kapatıyordu: görünen altı pikselin ikisi yanıt veriyordu ve okuyucunun gördüğü tutma çizgisi ölü kısımdaydı; oraya yapılan tıklama bu sütunu genişletmek yerine yandaki sütunu sıralıyordu. Şimdi hücrenin içinde on piksel, 1440 ve 360 piksel genişlikte onunun onu da imlece yanıt veriyor.
- Sürükleme, `mousemove` / `mouseup` çiftini basışta bağlıyor ve bırakışta çözüyor. Kalıcı bağlandıklarında ışık kutusu her açılışta bir çift ekliyordu (on açılış-kapanışta 1'den 11'e; her biri o açılışın kopyalanmış görselini tutuyordu), yakınlaştırılabilir grafik her grafik için bir çift (grafikler sayfasında 10), genişletilebilir tablo her sütun için bir çift (tablolar sayfasında 14). Üçü de artık boştayken 1 ya da 0. Grafik ayar penceresi ile kaydırma takibi de aynı geçişte denetlendi; onlar eleman başına değil, bir kez bağlanıyor.
- Markdown görüntüleyicisinde açılan belge okunur, çalıştırılmaz. Bağlantı verilen bir `.md` içindeki `<script>` ana sayfada çalışıyordu: genel bir değişken atıyor, `document.title` değerini değiştiriyor ve kitin kendi `oku-theme-mode` anahtarını localStorage'a yazıyordu. Betikler, `on*` işleyicileri, `javascript:` hedefleri ve çerçeve ögeleri dosya sayfaya ulaşmadan çıkarılıyor, görüntüleyici de kaçının çıkarıldığını söylüyor. Sayfanın kendi adaları değişmedi.
- Kaynak gösterme türünün rengi artık bir belirteçten geliyor, yani temayı izliyor. `paper` türü hem kart simgesinde hem alıntılanan sözcüklerin altındaki çizgide `#4338ca` boyuyordu — `--series-3`'ün açık tema değeri — ve koyu sayfada dört kardeşi ters dönerken o açık kaldı: kendi simge zemininde 2,05:1, ötekilerde 8,7 ile 14,8 arası. Şimdi 8,35:1.
- Yarıda kalan bir derleme yalnızca yeni sayfalara mal olur: her sayfa önce `dist/.build-<pid>/` içine yazılır, en sonda yerine taşınır. Daha önce yarıda kalan bir derleme, üretilmiş 120 dosyanın 120'sini siliyordu.
- Yazı tipleri sayfayla birlikte gelir: Inter ve JetBrains Mono dört değişken woff2 olarak pakete alındı, `_oku/vendor/fonts/` altından paylaşılır. Tek dosyalık bir sayfa açılışta Google'a üç istek yapıyordu; artık hiç yapmıyor.
- Blok + satır içi ayrıştırıcı üzerinde GFM uyumluluk geçişi: ters eğik çizgi kaçışları, sözcük içi alt çizgi kuralı, görseller, üstü çizili metin, otomatik bağlantılar, bağlantı başlıkları, karakter başvuruları, sert satır sonları, `***` / `___` çizgileri, `~~~` çitleri, ATX kapanış dizileri, sıralı liste başlangıcı, çok paragraflı liste maddeleri, tablolarda kaçırılmış dikey çizgiler, sütun hizalaması, düzensiz satırların normalleştirilmesi, benzersiz başlık çapaları.
- Dipnotlar ve başvuru biçimli bağlantılar, b[] dizeleri boyunca sayfa genelinde çözülüyor; `oku check` tanımsız bir başvuruda uyarıyor.
- Standart docs/ yerleşimi için site gezintisi: manifest kitin indiği yere yazılıyor ve kendine ait sayfası olmayan bir düzey artık ağacı bozmuyor.
- `oku serve`, init dizininin dışındaki sayfalar için kite geri düşüyor ve istek yollarındaki yüzde kodlamasını çözüyor (ASCII dışı dosya adları).
- Kaynak biçimi v3 (markdown öncelikli) uçtan uca yayımlandı; v2 sayfaları için yapısal denetim yeniden ayağa kaldırıldı.
- Kaynak biçimleri, gerçekten yayımlanan ikiye indirildi: `.md` (yazarın yazdığı) ve `.json` (markdown biçimi ortaya çıkmadan önce yazılmış sayfalar). Ölçümlü karşılaştırma için taşınan üçü — html öncelikli, asciidoc, djot — derlemleri ve gidiş-dönüş testleriyle birlikte silindi. HTML, adın çağrıştırdığı anlamda hiçbir zaman adaylardan biri olmadı: o, her derlemenin ÇIKTISIDIR.
- Yol tabanlı gezinme yönlendiricisi — URL yolu ile çizilen sayfa artık ayrışamıyor; eski karma bağlantılar normalleşiyor.
- Geniş şemalar sütunlarına her zaman sığıyor: SVG en fazla yazıldığı boyutta çizilir ve sütuna göre küçülür; viewBox'ı da mermaid'in kutusu ile gerçek içerik sınır kutusunun birleşimine göre yeniden ayarlanır, böylece mermaid'in o kutunun dışına yerleştirdiği hiçbir şey kesilmez. Geniş bir şemada ayrıntıyı okumak lightbox'ın işidir. Bu, yazılan boyutun %90'ını taban alan eski davranışın yerini aldı; geniş akış şemalarının taşmasına ve kırpılmış görünmesine yol açan oydu.
- Lightbox: görüntü alanının tamamını kaplayan çerçeve; şemalar da grafikler gibi canlı öğeyi taşıyor (etkileşim tümüyle korunuyor).
- Renderer'da satır içi arındırılmış HTML izin listesi geri getirildi; sunburst birinci halka için gösterge çipleri kazandı.
- Tarayıcı gerileme takımı (gerçek serve işleyicisine karşı başsız chromium) varsayılan pytest koşusuna bağlandı.

Bu döngüde yayımlanan öne çıkanlar. İşleme başına tam geçmiş git log içinde.

```oku-kpi-grid
{"tiles":[{"num":"53","label":"Şemanın kabul ettiği grafik türü"},{"num":"7","label":"Eşlenik imleç süpürmesi olan grafik"},{"num":"10","label":"Grafikler sayfasında amaca göre gruplanmış aile"},{"num":"0","label":"Quadrant Playwright taramasında görünür etiket-köşe çakışması"}]}
```

- Aileye göre seçim yeniden yazıldı — canlı küçük çizimler taşıyan 43 tıklanabilir mini kart; tıklama doğrudan o türün tam örneğine götürüyor.
- Grafikler sayfası 10 amaç ailesine göre yeniden gruplandı (Kartezyen / Kategorik / Parça-bütün / Dağılım / Eğilim / Akış / Ağ / Çok değişkenli / Hedef / Coğrafi).
- Kod / çıktı hizalaması her yerde sıfır piksel — her belge sayfasındaki her örnek çiftinde kutular üstten hizalanıyor.
- Bilgi balonunun genişliği sabitleme durumları arasında değişmiyor; sabitlenmiş balon bir × kapatma düğmesi alıyor; görüntü alanına sabitlenmiş konumlandırma onu tam ekran lightbox'ın üstüne çıkarıyor.
- Kartezyen grafikler eşlenik dikey kesik çizgi imleci ve imlecin en yakın x konumundaki her seri değerini listeleyen konuma özel bir bilgi balonu kazandı.
- Ridgeline + sparkline imleç işleyicileri zengin bilgi balonunu imleç konumuna göre güncelliyor (önceden yalnızca girişte sabitti).
- Quadrant köşe etiketi ve Kartezyen gösterge kümesi, veri etiketleri için uçuşa yasak bölge sayılıyor — `_deconflictLabels` bu bölgelere düşecek etiketleri kaydırıyor ya da gizliyor.
- Bubble göstergesinin arka planı tümüyle donuk — devasa bir baloncuk artık çip etiketlerinin arasından sızamıyor.
- Parallel-coordinates çizgisi dururken 3.6px, imleç altında 5px; imleçle birlikte ötekiler soluyor, böylece vurgulanan kayıt öne çıkıyor.
- Donut / pie'da sabitlenen dilimler büyüyor ve vurgu çizgisi alıyor; grafik biçimlerinde genel odak çerçevesi kaldırıldı.
- Gauge bölgeleri isteğe bağlı etiketler ve imleçle sürülen konum-değeri inceleyicisi kazandı.
- Mermaid kırpılma önlemi: `.okd-render` artık overflow:hidden, en-boy uyumu uzun dikey şemaları 70vh ile sınırlıyor, tüm SVG metinlerinde yazı boyutu 13px'e sabitlendi.
- Compare-grid kartları isteğe bağlı `href` kazandı — kartın tamamı tıklanabilir. Aileye göre seçim bunu kullanıyor.
- Tablolarda çok satırlı hücre içeriği için sütun başına `wrap: true`.
- Yüzeydeki bütün html-doc göndermeleri kaldırıldı (eski ad yalnızca GitHub URL'sinde kaldı).

## Kilitlenmiş kararlar {#decisions}

Her biri bir zamanlar açık bir soruydu; gelecekteki katkıcılar yeniden tartışmasın diye burada toplandı.

- Proje adı = oku (Türkçe buyrum kipi). Her katman `oku` okur: paket (src/oku/), kit CSS önekleri (okt- / okc- / okd-), JS küresel değişkenleri (__oku*), olay adları (oku:*), veri öznitelikleri (data-oku-*) ve custom element etiket adları.
- GitHub uzak adresi github.com/mmdemirbas/html-doc olarak kalıyor — şema $id'si, git-clone örneği ve jsdelivr CDN adresi hep oraya bakıyor. Yerel depo dizininin adı uzak adresi izliyor.
- Örnek çifti içindeki kod / çıktı sütunları kutunun üst kenarında hizalanmak ZORUNDA. Kod olmayan çıktılar için 24px'lik boşluk yok — kod ve çıktı sütunlarının görünen üst kenarları, çıktının türü ne olursa olsun aynı hizada durur.
- Bilgi balonunun genişliği sabitleme durumları arasında değişmez. Sabitli ile sabitsiz arasındaki fark alt yazı metni ve kapatma düğmesinin görünürlüğüdür, ASLA sınır kutusu değil.
- ext-ref sunucu adı bir gezinme bağlantısı DEĞİLDİR. Tıklamak bilgi balonunu sabitler; hedef adres, kaynak kartının içinde tıklanabilir bir alan adı bağlantısı olarak durur. Öğe başına tek işlev.
- Pagefind bir sistem ikilisi değil, bir Python bağımlılığıdır — `oku[search]` eki `pagefind[bin]>=1.5` çeker.
- Yerleşim = A seçeneği (ortalanmış belge, üç kademeli içerik genişliği döngüsü: narrow / comfortable / max).
- Üçüncü kuşak görseller (sankey / network / scatter-matrix / parallel-coordinates / chord / geo) kitin kendi grafik türleri olarak yayımlanıyor — üçüncü taraf grafik kütüphanesi yok.
- Grafikler / tablolar / şemalar, Başvuru'nun alt sayfalarıdır (meta.parent: 'reference').
- Kaynak biçimi = v3, markdown öncelikli. Bir sayfa `.md` dosyasıdır: YAML ön verisi + katı GFM gövdesi (girintili kod bloğu yok, setext başlık yok, gevşek devam yok — denetlenir, yine de geçerli GFM'dir); kit bileşenleri, gövdesi tek satırlık derli toplu bir JSON olan ```oku-<tür> çitleridir; şemalar, altına italik bir açıklama satırı gelen ```mermaid çitleridir; blok düzeyindeki ham HTML adaları (script dâhil) kite dokunulmadan geçer ve `oku check` tarafından denetlenir. Karar, belirteç sayımı (markdown, v2 JSON'dan ≈ %20 daha yalın, HTML'den çok daha yalın), LLM üretim doğruluğu ve ekosistemin yönü ölçüldükten sonra verildi; HTML öncelikli biçim değerlendirildi ve depoda duran, sürekli düzenlenen kaynaklar için elendi. JSON sayfaları (v1/v2) uyum katmanlarıyla çizilmeye devam ediyor; `oku migrate` onları dönüştürüyor.
- Derleme çıktıları = iki ağaç (standalone, site). dist/markdown ikizi yok — `.md` kaynakları yapay zekâ/LLM için kanonik yüzeydir; llms.txt site belge kökünde durur.

## İlkeler {#principles}

Okuyucuya, yazara ve bakımcıya bakan, her değişikliğin uyduğu kurallar.

- Tek bir SOL yan panel — içindekiler + site ağacı üst üste. Sağda içindekiler yok, kenar ortasında sekme yok.
- Görsel öncelikli yazım. Artı/eksiler → karşılaştırma kartları, ölçümler → kpi ızgarası, ödünleşimler → çipli tablo. Paragraf + madde imi varsayılan değil, geri düşülen seçenektir.
- Her düzeltme bir Playwright denetimiyle birlikte gelir. Tarayıcı düzeyindeki düzeltmeler, işleme gövdesine yazılan sayısal doğrulamalarla yayımlanır.
- Sayaç ve rozetlerde yalnızca sayı — kitin içine gömülü İngilizce sözcük yok.
- Ayrı gösteri sayfası yok. Bir bileşene ait her örnek kendi başlığının yanında durur.
- Belgelerde süreç / tur / tarihçe göndermesi yok — nasıl bu hâle geldiği değil, şu anki davranış anlatılır.
- Yerleşim değişmezleri göz kararı değil sayısaldır — kuralı bir boundingBox / hesaplanmış stil doğrulaması olarak yazın ya da henüz bir kural olmadığını kabul edin.
